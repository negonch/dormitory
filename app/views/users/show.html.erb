<!DOCTYPE html>
<html>

<div class="header">
  <div class="container">

    <div class="header-nav">

      <%= link_to 'Войти', log_in_path, class: 'button button-light button-small' %>
    </div>
  </div>
</div>


  <div class="container">
    <% if flash.any? %>
      <div class="content-wrapper buffer-both">
        <% flash.each do |name, message| %>
          <%= content_tag :div, message, class: "flash-#{name}" %>
        <% end %>
      </div>
    <% end %>
    <%= yield %>
  </div>



<div class="content-wrapper buffer-both text-center">
  <div class="buffer-both">
    <h1 class="title-medium">ФГБОУ ВО СПбГПМУ Общежитие №1</h1>
  </div>
</div>


</html>
